# README 配图

这些图片用于仓库首页的项目介绍，使用项目内的公共示例内容。

| 文件 | 内容 |
| --- | --- |
| `homepage-desktop.jpg` | 运行中的主页桌面端首屏 |
| `homepage-mobile.jpg` | 运行中的主页手机端首屏，390 × 844 的浏览器视口 |
| `editor.jpg` | 可视化编辑器的图层、画布与个人介绍设置 |
| `markdown-editor.jpg` | 新增 Markdown 模块后的源码编辑界面 |
| `workflow.svg` | 可编辑的「编辑 → 保存 → 构建 → 发布」流程图 |

截图更新：2026-09-20。界面截图来自当前源码的本地运行结果；`design/` 中的设计稿另行保留，不作为运行截图。

更新截图时，启动 `npm run editor`，打开 `/` 或 `/__edit/`，等待字体与图片加载完成再截图。使用示例数据副本进行演示，避免覆盖自己的页面内容。手机端使用 390 × 844 的视口；桌面与编辑器使用常规桌面视口。保留原始界面，不在截图中合成不存在的控件。
